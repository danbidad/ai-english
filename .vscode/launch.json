{
    "configurations": [
        {
            "command": "npm run start",
            "name": "Run npm start",
            "request": "launch",
            "type": "node-terminal",
            "cwd": "${workspaceFolder}/server"
        },
        {
            "command": "npm run dev",
            "name": "Run npm dev",
            "request": "launch",
            "type": "node-terminal",
            "cwd": "${workspaceFolder}/server"
        }
    ]
}